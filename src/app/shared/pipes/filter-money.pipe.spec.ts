import { FilterMoneyPipe } from './filter-money.pipe';

describe('FilterMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
